#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Return: Always 0 when successful
 */
int main(void)
{
	char b;

	for (b = 'a'; b <= 'z'; b++)
		putchar(b);
	putchar('\n');
	return (0);
}
